# Semantics setup

Declare Contract-B metrics in this directory. The starter uses the fixture backend;
choose `cube` and add `semantics/cube` for a Cube model. Add at least one policy to
a metric when your data needs masking, denial, or row filtering.
